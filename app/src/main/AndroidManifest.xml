<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.dzakdzak.firstkotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".HitungBangunDatar" />
        <activity android:name=".MVP.MainMVPActivity" />
        <activity android:name=".LATIHANMVP.KumpulanRumusActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LATIHANMVP.Segitiga" />
        <activity android:name=".LATIHANMVP.Persegi" />
        <activity android:name=".LATIHANMVP.Rumus" />
        <activity android:name=".LATIHANMVP.PersegiPanjang"></activity>
    </application>

</manifest>